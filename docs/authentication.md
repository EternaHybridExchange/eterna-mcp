# Authentication

This document is archived.

The old `register_agent` API-key flow is stale. Current Eterna AI MCP integrations use OAuth in supported clients and the code-execution MCP model:

- `execute_code`
- `search_sdk`
- `search_examples`

Use the canonical docs in `eterna-ai`:

- https://github.com/EternaHybridExchange/eterna-ai/blob/main/docs/mcp.md
- https://github.com/EternaHybridExchange/eterna-ai/blob/main/docs/sdk.md
